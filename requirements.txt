# RED STUDIO - Pinned Dependencies (Fix #23)
# All versions explicitly pinned to prevent silent breaking changes

Flask==3.0.2
Werkzeug==3.0.1
bcrypt==4.1.2
firebase-admin==6.5.0

# PDF generation - required for invoice download feature
# Install manually if missing: pip install reportlab==4.0.9
reportlab==4.0.9
openpyxl==3.1.5
gunicorn==21.2.0

